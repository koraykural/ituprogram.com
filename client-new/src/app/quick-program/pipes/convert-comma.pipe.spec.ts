import { ConvertCommaPipe } from './convert-comma.pipe';

describe('ConvertCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
