import { SmoothHeightDirective } from './smooth-height.directive';

describe('SmoothHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new SmoothHeightDirective();
    expect(directive).toBeTruthy();
  });
});
