import { ConvertLineBreakPipe } from './convert-line-break.pipe';

describe('ConvertLineBreakPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertLineBreakPipe();
    expect(pipe).toBeTruthy();
  });
});
