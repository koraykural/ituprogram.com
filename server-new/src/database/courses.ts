import { CourseBasic, CourseDetails } from '../interfaces';

export const getCourseBasics = async (_codes: string[]): Promise<CourseBasic[]> => {
  return [];
};

export const getCourseDetails = async (_codes: string[]): Promise<CourseDetails[]> => {
  return [];
};
